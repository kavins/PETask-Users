package com.petask.petask_users.utils;

import android.support.v7.widget.RecyclerView;
import android.view.View;

/**
 * A simple ViewHolder which extends RecyclerView's ViewHolder
 */
public class SimpleRVHolder extends RecyclerView.ViewHolder {
	public SimpleRVHolder(View itemView) {
		super(itemView);
	}
}
